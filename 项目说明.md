# Hanako-LAAP 项目说明

Hanako-LAAP 是 Hanako 桌面/Web Agent 与 LAAP 认知架构的源码集成项目，提供本地对话、工具调用、记忆、模型 Provider 和 PSI 认知边界。

## 核心架构

```text
用户输入
  → Hanako Web/Desktop Chat
  → Session / hub.send()
  → PSI before_turn
  → AGIAgent / PSIDriver
  → LLM Provider（如 DeepSeek）
  → PSI after_turn
  → 用户可见回复
```

AGIAgent 是默认认知 Runtime；BridgeCognitiveRuntime 保留为兼容回退实现。

## 主要能力

- Web 与 Desktop Chat
- Session 创建、恢复和持久化
- OpenAI-compatible LLM Provider
- DeepSeek 等 Provider 配置
- 文件、命令、搜索等工具调用
- PSI 认知边界与 turn receipt
- AGIAgent 的 needs、emotion、attention、世界模型和长期记忆
- Sidecar 意识总线
- Skills、MCP 和插件扩展
- Bridge 平台适配

## 快速安装

Windows 推荐在仓库根目录运行：

```bat
自动安装依赖.bat
```

安装开发测试依赖：

```bat
自动安装依赖.bat --with-dev
```

仅检测环境：

```bat
自动安装依赖.bat --dry-run
```

详细说明见：

```text
BOOTSTRAP.md
```

## 运行环境

- Python 3.11+
- Node.js 24.12.x–24.x
- npm
- Windows 原生依赖需要允许 npm lifecycle scripts

## Provider 配置

不要把 API Key 写入仓库。请通过本地环境变量或 Hana 设置界面配置，例如：

```powershell
$env:LAAP_PROVIDER="deepseek"
$env:LAAP_API_KEY="<your-key>"
$env:LAAP_BASE_URL="https://api.deepseek.com/v1"
$env:LAAP_MODEL="deepseek-chat"
```

## Runtime 选择

默认使用 AGIAgent：

```text
AGIAgent → PSIDriver → PSI → LLM 表达通道
```

如需兼容回退，可显式设置：

```powershell
$env:LAAP_COGNITIVE_RUNTIME="bridge"
```

系统不会在未明确授权时静默切换 Runtime。

## 可选扩展

Hermes、model_tools、Agent-Reach 和 Swarm 都不是核心运行依赖。它们可按实际需求单独接入；缺失时不影响基础 Chat、AGIAgent、PSI 和直接 Provider 路径。

## 上传注意事项

- 不要上传 `.venv`、`node_modules`、日志、Session、数据库或本地运行状态。
- 不要上传 API Key、Token、设备凭据或私有配置。
- 上传前应额外扫描 Git 历史中的敏感信息。
- 本项目源码优先，不以 Electron 安装包或发行构建作为运行前提。
